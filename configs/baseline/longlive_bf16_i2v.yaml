# Original LongLive 2.0 sampling; no camera Adapter and no separate LoRA.
# All relative paths are independent of terminal cwd.
paths:
  workspace_root: null  # auto: workspace/project/StreamAdapter -> workspace
  data_root: public_data/RealCam-Vid/RealEstate10K
  model_root: pretrained_model/LongLive
base_model_dir: ../Wan2.2-TI2V-5B  # relative to paths.model_root

model_kwargs:
  model_name: Wan2.2-TI2V-5B
  timestep_shift: 5.0
  num_frame_per_block: 8
  local_attn_size: 32

i2v: true
use_ema: false
num_samples: 1
num_output_frames: 32
save_latents_only: false
save_with_index: true
inference_iter: -1
merge_lora: false
model_quant: false
fp8_quant: false
torch_compile: false
num_train_timestep: 1000

data:
  data_path: ${paths.data_root}/baseline_i2v  # prepared images + matching .txt; not CSV
  image_or_video_shape: [1, 32, 48, 44, 80]
  chunks_per_shot: 0
  scene_cut_prefix: "The scene transitions. "
  allow_padding: false
  min_latent_frames: 0
  uniform_prompt: true
  max_chunks_per_shot: 0

inference:
  sampling_steps: 4
  independent_first_frame: true
  sink_size: 8
  guidance_scale: 1.0
  multi_shot_sink: true
  multi_shot_rope_offset: 8
  kv_quant: false
  kv_quant_scale_rule: mse
  kv_quant_backend: cuda
  streaming_vae: false
  async_vae: false
  vae_type: wan
  vae_device: null
  use_relative_rope: false
  inference_t_scale: null

checkpoints:
  generator_ckpt: LongLive-2.0-5B/model_bf16.pt  # relative to paths.model_root

logging:
  seed: 0
